# Prompt interno — Criar Testes

```
1. Localizar a suíte de testes existente e o padrão usado (indexes/mapa-indices.md → Testes automatizados).
2. Priorizar cobertura dos fluxos críticos (architecture/visao-geral.md) e de permissões (architecture/auth-e-permissoes.md) antes de casos de borda triviais.
3. Para código de integração (Orthanc/HL7), incluir teste de comportamento de falha (não só do caminho feliz).
4. Seguir a convenção de nomenclatura/estrutura já usada no projeto — não introduzir um segundo padrão de teste.
```
