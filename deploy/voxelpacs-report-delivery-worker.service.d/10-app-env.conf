# O systemd lê este arquivo antes de reduzir privilégios para o usuário de serviço.
# O processo não precisa ler o .env diretamente, preservando permissões root-only.
[Service]
EnvironmentFile=/var/www/voxelpacs/app/.env
